# SAX 4 Miler Leaderboard

Hosted on Azure Static Web Apps from this repo.

## Local
```bash
python3 -m http.server 8765
```

## Deploy
Push to `main`. Workflow uses secret `CAY_SECRET` (SWA deployment token).
